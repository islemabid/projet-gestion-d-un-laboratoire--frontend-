export interface Member {
    id: string;
    cin: string;
    name: string;
    createdDate: string;
    cv: string;
    type: string;
}
export interface Members {
    id: string;
    cin: string;
    nom: string;
    dateNaissance: string;
    cv: string;
    prenom: string;
}